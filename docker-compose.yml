version: "3.9"

services:
  chat-as-a-key:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./sessions:/app/sessions
    restart: unless-stopped
    shm_size: "256m"
